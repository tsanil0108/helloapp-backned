package com.packersmovers.marketplace.common.enums;

/** Core marketplace roles. Admin's fine-grained module permissions are in AdminPermission. */
public enum RoleName {
    SUPER_ADMIN,
    ADMIN,
    PROVIDER,
    CUSTOMER
}
